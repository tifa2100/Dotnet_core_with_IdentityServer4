
@{
    ViewData["Title"] = "Secure";
}

<h1>Secure</h1>

<h3>User Claims</h3>

<dl>
    @foreach (var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dt>@claim.Value</dt>
    }
</dl>